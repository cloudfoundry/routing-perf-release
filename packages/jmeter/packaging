# abort script on any command that exits with a non zero value
set -e -x

cd ${BOSH_INSTALL_TARGET}
tar zxvf ${BOSH_COMPILE_TARGET}/jmeter/apache-jmeter-3.0.tgz
if [[ $? != 0 ]] ; then
  echo "Cannot unpack Jmeter"
  exit 1
fi

ln -s apache-jmeter-3.0 apache-jmeter

cd ${BOSH_INSTALL_TARGET}
