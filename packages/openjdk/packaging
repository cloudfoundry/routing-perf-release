# abort script on any command that exits with a non zero value
set -e -x

ls -la .
ls -la openjdk
mkdir ${BOSH_INSTALL_TARGET}/jdk && \
  tar zxvf openjdk/openjdk-1.8.0_91-x86_64-trusty.tar.gz -C ${BOSH_INSTALL_TARGET}/jdk
if [[ $? != 0 ]] ; then
  echo "Cannot unpack JDK"
  exit 1
fi
